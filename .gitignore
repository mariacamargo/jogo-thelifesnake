__pycache__/
*.pyc

# Foto/musica pessoal usada pelo jogo desktop (assets/face.*, assets/musica.*)
# fica de fora do repositorio por padrao - so no seu PC.
/assets/*
!/assets/.gitkeep

# artefatos gerados pelo pygbag ao construir a versao web
# (o build publicado que importa fica versionado em docs/)
web/build/
